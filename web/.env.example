NEXT_PUBLIC_TASKFLOW_API_URL=http://localhost:3000
# Optional: only needed when Nginx exposes the REST API under a path such as /api.
NEXT_PUBLIC_TASKFLOW_SOCKET_URL=http://localhost:3000

# Set to true only when API cookies are hosted on a different site.
DISABLE_PROXY_AUTH=false
