PORT=3000

DB_HOST=
DB_PORT=5432
DB_USERNAME=
DB_PASSWORD=
DB_NAME=

DATABASE_URL=

JWT_SECRET=
JWT_EXPIRES_IN=
JWT_REFRESH_SECRET=
JWT_REFRESH_EXPIRES_IN=
CORS_ORIGIN=http://localhost:3001
# Set to a shared parent domain, such as .example.com, only when frontend and API use subdomains.
COOKIE_DOMAIN=
# Use `none` only when the frontend and API are intentionally hosted on different sites.
COOKIE_SAME_SITE=lax
